function searchBarFunc () {
    let fromBar = document.getElementById('fromBar');
    let toBar = document.getElementById('toBar');

    if (fromBar.value == toBar.value) {
        alert("wip..");
        return;
    } else {
        alert("wip...");
    }

    /*let cpt = 0;
    while (fromBar != document.getElementsByName(travelOrigin).innerHTML) {
    }*/
}
